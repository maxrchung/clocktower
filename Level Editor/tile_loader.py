__author__:"Dustin"
import pygame
import tile_editor
import sys
import os

class Level:
	def __init__(self, screen, matrix):
		self.window = screen
		self.matrix = tiles

	def parser(self):
		for row in self.matrix:
			for col in row:
				if col == "X":
					pass
				elif col == ""
